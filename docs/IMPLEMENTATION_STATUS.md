# 平台直采迁移：开发与验收状态

更新日期：2026-09-15。独立分支：`codex/tikhub-free-collectors`。

**项目尚未全平台完成。当前 2／14 个目标账号通过真实接入验证；其他账号仍需授权登录、完成平台映射及实测。**

小红书安芯日记另已通过内容采集验证：62篇、351粉丝、五项累计指标完整；评论详情要求主站独立登录，尚未计入完整接入账号数。

## 已交付代码

- 视频、图文、评论及周期性新作品发现统一使用 provider，运行代码不再依赖 TikHub Key 或付费请求路径。
- 集成主干的官方回复时间线、稳定用户 ID 识别、内容分层轮询及前端页面；主干分支本身未被修改。
- 视频号原生后台适配已完成望获OS验证：103条目录、32条评论及回复、跨会话ID一致性和历史标识冲突保护，详见 [视频号适配记录](WECHAT_CHANNELS_ADAPTER.md)。
- B站原生后台适配：身份核验、粉丝数、已通过稿件分页、逐作品评论和回复分页；同一作品的评论与回复复用一次后台扫描。
- 小红书创作后台适配：精确小红书号核验、网页签名响应监听、滚动容器分页、置顶去重、首屏总数校验及可移植会话。详见 [小红书适配记录](XIAOHONGSHU_ADAPTER.md)。
- 公众号官方发布列表适配，以及稳定令牌缓存/自动续期；续期使用普通模式，不强制使其他令牌失效。权限、白名单和管理员确认错误单独报告。
- 浏览器通用适配支持分页、原生只读 GET、CSV/XLSX 自动下载与解析、多图文 JSON 展开、账号锁、独立会话及受限可移植会话。
- 显式内容/评论 ID 映射、精确链接对账、缓存字段来源标记。没有稳定 ID 或映射存在冲突时停止覆盖。
- 本地账号接入面板支持打开独立登录、配置和只读验证，仅监听回环地址，写操作有请求来源检查。
- 数据与评论分开调度，支持错过每日时间后的补跑、失败延迟重试、任务超时和运行目录单实例锁。
- 健康检查区分身份、内容完整性、必要指标、评论及回复证据；记录实际运行结果并提供观察期门槛。

## B站真实验证

账号：望获OS。已用网站返回的稳定 UID 核实身份。

| 验证项 | 实际结果 |
| --- | --- |
| 后台已通过稿件 | 完整分页读取 89 条；这是后台已通过稿件范围，不把它混称为公开空间视频数 |
| 粉丝数 | 验证时返回 2,087 |
| 一条代表作品评论 | 3 页，共 27 条：11 条一级评论、16 条回复 |
| 采集 CLI | 在私有测试目录刷新目标账号；其余账号保留旧快照 |
| 评论 CLI | 对两条作品建立启动基线，未将历史评论作为新增提醒 |
| 三套快照校验 | 视频、图文、评论时间线通过 |
| 浏览器重启恢复 | 关闭专用 Chrome 后重新启动采集，身份仍匹配并读取 10 条作品 |
| 可移植会话 | 只导出B站域的会话；新浏览器上下文仍能核验身份并读取作品。仅在本机验证，尚未证明跨机器/IP 可用 |

所有真实结果仅写入 `.runtime/`，未覆盖主项目快照，未发送实际邮件。

## 本地测试

- 107 项自动测试通过：包含真实 Chrome 响应/下载夹具、小红书容器滚动与首屏总数、身份与指标映射、B站原生数据结构、评论与回复、主干时间线、凭证续期、ID 映射、缓存保护、调度、控制面板来源限制及 CLI 隔离。
- 浏览器夹具不访问真实平台；网络与 SMTP 在离线测试中被阻断，不能将这些夹具描述为其他平台实测。
- Python 编译、三套前端 JS 与管理页内联 JS 语法检查通过。
- 独立本地 Compose 配置解析通过。没有启动、重建或停用 Docker 服务。
- 测试运行器检查受版本管理文件的内容保持不变。

## 仍未完成

| 对象 | 当前状态 |
| --- | --- |
| B站国科安芯 | 适配代码可复用，未完成该账号登录与身份实测 |
| 视频号 | 望获OS已通过；国科安芯、安芯很安心仍需各自登录核验。旧标识冲突的两条缓存另行保留待复核 |
| 抖音 2 个账号 | 待登录并实现/核验其真实后台流程；不把旧版开放平台接口当作无条件可用 |
| 知乎 2 个账号 | 匿名访问实测被拒绝；待登录校准 |
| 小红书 1 个账号 | 安芯日记创作后台已验证62篇、5项累计指标及351粉丝；无界面会话与采集CLI通过。主站评论页面另需扫码，评论/回复适配尚未验收 |
| 公众号 4 个账号 | 官方接口代码及续期测试已完成；未获得实际账号凭证，后台导出流程也待登录校准 |
| 其他平台的实际报表导出 | 通用下载/解析能力已实现，真实按钮、字段及导出范围尚待核验 |
| 七天观察 | 尚未开始，不具备发布验收结论 |

通用适配器和测试通过不能代替真实平台接入。目前健康检查明确报告 `release_ready=false`，不将此分支宣称为全平台上线版本。

## 主干隔离

开发、提交和合并均在本工作树及独立分支内。已将主干的官方回复时间线改动集成到本分支；没有反向合并、推送或部署。主干仍保留自己的并行提交。

接入和测试方法见 [本地开发说明](LOCAL_DEVELOPMENT.md)。
