# 平台直采迁移：本地开发状态

日期：2026-09-15。分支：`codex/tikhub-free-collectors`。状态：公共层及运行入口已改造，真实账号接入待继续，不具备全平台上线条件。

## 本轮完成

- 删除视频、图文和评论模块内的 TikHub 客户端与全部付费请求路径，解除 Key 必填要求。小时级作品发现使用同一个 provider。
- 建立受限 HTTP、持久化浏览器会话、账号锁、身份比对、页面响应采集、分页终止校验、字段与统计口径映射。
- 实现 B站公开视频详情适配，公开数据仅补空缺，不覆盖已有后台统计。
- 实现公众号官方发布列表分页、多图文拆分、URL 到历史 ID 的映射；新增独立每日阅读人数查询方法，未将它并入累计阅读量。
- 保留缺失指标的历史值并标记缓存来源；不完整页面不推进评论状态；新旧评论 ID 未核实则要求迁移核验。
- 增加专用运行目录、测试模式、邮件阻断、状态检查/登录/只读验证 CLI 和独立的本地 Compose 配置。

## 已执行验证

| 检查 | 结果 | 范围 |
| --- | --- | --- |
| `.venv/bin/python scripts/test_local.py` | 52 项通过 | 所有 requests 外部 HTTP 与真实 SMTP 默认被阻断 |
| 真实 Chrome 自动化夹具 | 8 项通过，包含在上述 52 项中 | 页面响应均由本地夹具接管；不代表真实平台后台已验证 |
| CLI 集成 | 通过，包含在上述 52 项中 | 隔离目录中缺少会话时返回失败状态，两套原有记录数与指标保持，未生成提醒邮件 |
| Python 编译 | 通过 | pipeline、tests、scripts |
| 前端 JS 语法 | 两套脚本通过 | 前端未修改 |
| `git diff --check` | 通过 | 无补丁空白错误 |
| TikHub 静态检查 | 运行 Python 中无付费域名和 Key 引用 | 文档/测试仍保留历史说明与阻断测试字符串 |
| B站只读真实请求 | 成功 | 望获OS 的一个公开视频详情，视频标识和原作者 ID 均匹配；输出仅在 `.runtime/probes/` |
| 账号绑定清单 | 14 个待完成 | 当前没有绑定已核验的完整后台工作流 |

本轮测试未实际发送邮件，未启动、重建或停用任何 Docker 服务，也未更新原项目的大屏快照。

## 与主干的关系

本工作树从 `9779920` 建立独立开发分支。本轮没有向主目录写入代码、合并或部署。

开发期间主干由其他工作推进至 `be49ae2`，提交标题为 `feat: add budgeted official reply timeline`。主目录与此工作树的相关文件不是同一 inode，内容也不同。因此不能用“主干文件哈希始终不变”描述本轮隔离；准确结论是本轮修改在独立工作树，主干的并行提交保留。

将来集成时，需在本分支处理评论模块、快照校验等重叠变更，并回归主干新加入的官方回复时间线，不能覆盖它。

## 下一步必须完成

1. 先用 B站一个项目账号登录专用浏览器，捕获并核验后台身份、作品、一级评论及二级回复的真实响应和翻页操作。此前登录测试浏览器已断开，需重新打开专用会话。
2. 为其余平台逐账号绑定实际工作流和字段。统一浏览器引擎可复用，但尚无这些后台可直接使用的已验证采集配方。
3. 微信官方路线需绑定该公众号授权令牌；当前只有受控错误处理和本地响应测试，未完成官方权限的真实验证及令牌自动续期。
4. 如果需要后台报表导出，须在登录后实现实际导出/下载步骤；当前版本尚无导出适配。
5. 完成历史内容与评论 ID 对账。ID 不兼容时仍须实现映射迁移；当前只提供拒绝不安全迁移的保护。
6. 逐平台真实分页、故障恢复和调度性能验收；全部接通后再开始至少 7 天连续观察。

操作方法、参数与边界见 [本地开发说明](LOCAL_DEVELOPMENT.md)。
