# 账号授权与失效恢复操作

Docker初始化、端口和目录配置见 [Docker可视化授权](DOCKER_AUTHORIZATION.md)。本页说明日常操作，不替代真实部署环境的账号验收。

## 宿主机没有初始化工具时

可以直接使用登录镜像内的Python和htpasswd，不必给宿主机安装这些依赖。先准备 `.env` 并构建镜像，然后在交互终端运行：

```bash
docker compose --profile login build login
mkdir -p .runtime
docker run --rm -it --network none --user "$(id -u):$(id -g)" \
  --entrypoint python -v "$PWD/.runtime:/setup" \
  promotion-dashboard-login:latest /app/scripts/init_authorization.py \
  --auth-dir /setup/authorization --provider-dir /setup/provider-config \
  --sessions-dir /setup/sessions
```

已有本机旧配置时，在命令末尾加 `--migrate-provider-file /setup/providers.json`。新文件会改用portable会话并去除本机调试连接，原文件保持不变。初始化完成后再启动 `login`；该初始化容器不运行采集、桌面或网络服务。以后也可用相同容器命令加 `--replace-password --prompt-password` 重置口令。

## 首次授权

1. 打开管理地址并输入管理员凭证。默认地址为 `http://127.0.0.1:18762/`；远程机器先通过SSH转发访问。
2. 在账号清单选择目标账号，点击“打开登录”。页面显示服务器浏览器和授权截止时间。同一桌面一次只处理一个账号，其他账号的正常采集不受影响。
3. 在该窗口扫码或使用平台正常登录方式。小红书需在创作后台和主站两个标签页登录同一个账号；安全验证由运营人员正常完成。
4. 点击“已完成登录，验证并保存”。程序先核对项目绑定的账号ID，导出独立候选会话，再使用新浏览器恢复会话、验证完整内容与必要指标。四个评论明细平台还会验证代表作品的评论/回复读取。
5. 验证成功才替换正式会话并恢复该账号采集。账号授权状态与整体采集验收分开显示：例如完整但为空的评论样本允许保存登录态，不伪称已经验证二级回复。

公开采集的CSDN、电子发烧友、搜狐和今日头条不需要扫码。公众号官方API配置也不通过网页扫码续期，应维护其独立API授权。

授权服务只保存会话、配置、验证证据和请求预算，不更新大屏快照、评论基线或邮件队列。正式调度任务随后读取新会话并更新数据，不需要运营人员电脑持续开机。

视频号后台会话的 Cookie 到期时间不能证明服务器端登录仍有效。可单独启动 `session-maintenance` profile：它每小时检查一次，仅对已授权且至少 3 小时没有成功保存会话的视频号执行一次只读首页身份核验；核验通过后原子保存浏览器会话状态。它不采集大屏快照、不运行评论任务，也不连接 SMTP。启动命令：`docker compose --profile session-maintenance up -d session-maintenance`。本地隔离 Compose 使用相同 profile。检查结果位于 `data/session_maintenance_state.json`。电脑休眠、Docker 停止、平台主动撤销登录或服务器端固定有效期都无法通过续用检查绕过；这时仍需人工扫码，服务会跳过待重新授权的账号。先使用一个账号观察至少一个平台续期周期，再决定是否为其他账号启用。

授权管理页每个账号另有“邮件通知”入口，可选择沿用平台默认、单独开启并填写收件人、或关闭。它对公开采集账号也有效，与是否需要扫码授权无关。设置保存在共享的 `data/comment_notification_settings.json`（权限 0600），登录服务与采集/发送服务须挂载同一个 `data` 目录。评论监测按账号生成待发邮件；发送器发送前会重新核对设置，因此关闭账号或修改收件人也会作用于尚未发送的队列。此页面保存设置本身不会发送邮件，本地测试模式禁止连接 SMTP。

## 登录失效后

- 实际采集发现登录失效或账号身份不一致时，账号进入“需要重新授权”；后续自动任务暂停访问该账号，保留已有快照和评论状态。
- 点击该账号“重新授权”，重复正常扫码与验证保存。旧的成功会话在新候选验证完成前不会被覆盖。
- 若登录了错误账号、目录不完整、关键指标不足或小红书主站未登录，面板显示原因，允许继续完成登录后重试。仅打开窗口、导出文件或取得部分目录不算授权完成。
- 授权窗口有效期为30分钟。取消、超时或浏览器关闭会释放桌面，并继续等待重新授权；验证保存期间禁止重复提交或手工取消。
- 手工CLI的完整 `probe` 也可以重新验证并恢复已失效的会话；它不能抢占正在进行的可视化授权。导出、绑定和评论验证本身不会解除失效暂停。

## 服务重启和故障

配置、会话与验证证据提交前会保存私有事务备份。服务中途退出时，重启会回收经进程参数核验的本次浏览器/验证进程，并回滚未完成事务；已经完成的授权保留。新的授权操作不会被旧操作或迟到的失败结果覆盖。

如果存储权限或空间问题导致回滚失败，事务记录和备份会保留，服务不会开始新的授权覆盖它们。修复存储后重启授权服务，让恢复流程完成；日志和管理页面会报告恢复未完成。

更新项目账号名单或改绑稳定ID后，应重启授权服务并重新验证目标账号。不要把同名账号视作同一身份。

## 管理员口令重置

使用初始化工具的 `--replace-password --prompt-password`，保持原账号配置不变。随后单独重启 `login`，关闭旧的管理页面连接和已建立的远程桌面连接；不会重启采集器。

```bash
python3 scripts/init_authorization.py --replace-password --prompt-password
docker compose --profile login restart login
```

## 公众号历史发表记录

公众号发表记录可能包含失败的尝试。仅当平台明确返回未发布 `is_published=0`，且同时给出 `msg_status=6` 和“发表失败”说明，或 `msg_status=5` 和已核验的 `SENDFAIL_GETTOUINLIST_FAIL` 失败代码时，将该消息组记入 `excluded_publication_groups`，不作为已发布文章或已删除文章。失败尝试中的草稿 ID 不占用已发表文章 ID，避免同一草稿随后成功发表时被误判重复。未知、矛盾状态仍停止验证。

历史“审核中”记录须同时具备状态1、未发布标记和“审核中”标签，单列为待审核；“无法查看”状态8仅在全部子文章明确标记删除时按删除条目排除。旧版type16记录仅在明确已删除、带video_info且无图文子项时排除为视频消息。

旧版独立发表记录使用 `type=10002` 与 `publish_info`，不要求不存在的 `sent_info`。仅接受匹配消息ID、`publish_status=200` 且有有效 `create_time` 的记录，并记录时间字段来源。不能把更新、删除时间或当前时间补作发表时间。

只在当前授权操作的私有目录内启用发表目录断点缓存。重试先实际读取目录首页并核验账号身份、目录总数、首页消息顺序及发布状态，再复用已读页；目录或身份变化会丢弃旧缓存，缓存损坏则停止。重用页保留原获取时间，完整分页、稳定ID、业务状态与每日请求额度仍需通过校验。普通采集不使用此缓存；授权结束后随候选目录清理。

网页授权验证和图文采集命令的默认翻页上限为 500 页，显式 `--max-pages` 仍生效；身份、目录完整性、访问间隔和异常处理不变。

网页与平台直采默认不设每日总量上限（`DATA_DAILY_REQUEST_LIMIT=0`），仍按实际操作共享记账，这不是付费 API 费用计量。设置正整数可重新启用每日总量限制；已有部署若显式设置了800等旧值，应改为0后重新创建对应服务。关闭上限不会清零已用次数，也不会关闭平台访问间隔、身份核验或异常停止机制。

授权窗口默认有效期30分钟；大历史账号可在启动login服务前配置 `PROMOTION_AUTHORIZATION_TTL_SECONDS=3600`，最多允许1小时，最少5分钟。验证任务也受同一截止时间限制，不会无期限占用桌面。临时调高后应恢复默认。

仅在已启用正整数日上限时，经管理员明确同意，可在该部署的数据目录创建 `collection_budget_overrides.json`，按北京时间日期给出当天正整数上限，例如 `{"2026-09-17":1200}`。文件不存在或日期不匹配时仍使用 `DATA_DAILY_REQUEST_LIMIT`；每次实际操作继续共享记账。删除当天条目会恢复配置上限，已经发生的调用不会清零；若已用量超过恢复后的上限，当天后续调用会被拦截。显式设置0会忽略遗留的临时上限文件。临时文件已排除出 Git 和镜像构建上下文，不应复制到其他部署。

## 本地验证边界

隔离单测覆盖账号/操作ID匹配、来源和CSRF检查、候选会话恢复、失败回滚、超时、提交中断以及新旧授权代次隔离。Docker冒烟验证网关认证、桌面和WebSocket、挂载与重启；授权流程端到端测试使用模拟平台及真实Chromium会话：测试入口为空白页，目标域名强制指向容器本地地址，业务页面请求全部由夹具响应或拦截；没有使用真实账号或会话。这是目标域名与浏览器请求隔离，不宣称容器全面断网。

这些测试验证容器机制，不代表已经在生产服务器完成真实平台扫码。实际部署后仍须逐账号验证服务器IP、登录会话、权限及采集范围。


本轮验证（2026-09-16）：网关与共享目录26项检查、模拟平台授权生命周期44项检查全部通过。实际noVNC页面已连接并显示容器Chrome的模拟登录页；会话导出、独立浏览器恢复、失效暂停、重新授权及容器重启恢复均通过。测试前后镜像内大屏文件哈希不变，7类快照/提醒/调度文件未生成，测试容器已清理。
