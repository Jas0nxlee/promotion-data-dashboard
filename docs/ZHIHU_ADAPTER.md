# 知乎创作后台适配记录

核验日期：2026-09-15。账号安芯工程师，URL token `ezvdz-53`，与项目配置一致。

## 已验证结果

- 全量32篇专栏文章，4页目录；前后账号核验各1次，共6次原生读取请求。
- 32个文章ID与旧快照完全重合，无旧文章缺失。
- 阅读、赞同、评论、收藏均32/32有值；喜欢单独存于附加指标，分享保持未知。
- 会话导出后，独立无界面portable上下文再次采集，所有文章ID与逐篇四项指标一致；会话文件权限600。
- 图文采集CLI已在 `.runtime/local` 成功运行，主干和生产数据未修改。

## 读取边界

身份由已登录的 `/api/v4/me` 的 `url_token` 与内部ID核验。`is_active` 实测为时间戳，不能当作布尔登录标志。没有把账号邮箱或手机资料写入结果。

文章目录使用 `/api/v4/creators/creations/v2/article`，按网站实际的 offset、limit、时间范围、排序和协作参数读取，核对 `paging.totals/is_end/next` 及前后账号文章数。跨域、偏移异常、重复ID或数量变化均失败关闭。只采文章，不把回答、草稿混入原统计范围。

| 输出字段 | 后台字段 | 定义 |
| --- | --- | --- |
| read | reaction.read_count | 文章累计阅读 |
| like | reaction.vote_up_count | 原统计口径：赞同 |
| comment | reaction.comment_count | 累计评论计数 |
| collect | reaction.collect_count | 累计收藏 |
| extra_metrics.like_count | reaction.like_count | 喜欢，与赞同分开 |

后台列表未逐条返回作者时，仅记录“已核验账号的管理目录”，不推断文章原作者。粉丝数未取得，保持未知；未通过额外抓取公开主页硬凑字段。

公开个人页曾出现安全验证，采集暂停等待用户人工完成；完成后才恢复原生后台读取与会话恢复实测。遇后续验证仍需按平台要求处理。

知乎评论正文与回复不在原TikHub替代范围，本provider明确返回unsupported；既有评论数量增长提醒保持原范围。

## 待接入账号

土星云预期URL token为 `zhifxi5atvi`。其专用窗口此前实际登录安芯工程师，已停止读取目录；用户现指定留到实际运行时再登录正确账号。届时复用当前适配器并核验身份、权限与完整性，不仅按窗口名称认定身份，也不覆盖不匹配账号的旧快照。
