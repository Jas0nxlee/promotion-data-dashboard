# 数据大屏审计与指标说明

本文档记录视频、图文两套推广数据大屏的指标口径、数据粒度、采集保护和已知覆盖边界。页面展示与本说明保持同一口径。

## 1. 数据粒度

### 视频

- 原始粒度：一条记录表示“账号与视频的关联”。同一 B 站视频可能同时出现在两个账号列表中。
- 全局 KPI、平台分布、趋势、热榜和明细：按 `platform + video_id` 去重。
- 账号指标对比、账号概览：保留账号关联，便于衡量每个账号的内容覆盖。
- 共享视频会写入 `is_shared_content`、`primary_account_key` 和 `associated_account_keys`。

### 图文

- 原始粒度：一个账号下的一条平台发布记录。
- 跨账号去重只使用完全相同的规范化公开 URL；不能用公众号 `app_msg_id` 跨账号去重，因为该 ID 不是全局唯一。
- 相同标题发布到不同平台仍是不同内容记录。

## 2. 指标字典

| 指标 | 定义 | 缺失值处理 |
| --- | --- | --- |
| 作品 / 文章数 | 当前筛选范围内的去重发布数 | 无记录为 0 |
| 播放 / 阅读 | 平台公开返回的累计值之和 | 未公开显示 `-`，不计入合计 |
| 点赞 | 平台公开点赞累计值之和 | 未公开显示 `-`，不计入合计 |
| 评论 / 回复 | 平台公开评论口径；部分视频平台可能包含回复 | 未公开显示 `-`，不计入合计 |
| 分享 | 平台公开转发或分享累计值之和 | 未公开显示 `-`，不计入合计 |
| 收藏 | 平台公开收藏；视频号的“在看”等仅按上游映射展示 | 未公开显示 `-`，不计入合计 |
| 粉丝 | 账号资料在本次或最近成功采集时的快照值 | 未公开显示 `-` |
| 近 30 日发布 | 公开发布时间落在当前时间前 30 天内的去重发布数 | 缺少发布时间的内容不计入 |

每张总量指标卡同时显示“有值记录数 / 当前记录数”。业务线雷达使用有值记录的单条均值，并在提示框中给出各维度覆盖数；雷达值是业务线之间的相对归一化结果，不能当作绝对总量。

## 3. 数据源与覆盖边界

| 平台 | 采集方式 | 主要边界 |
| --- | --- | --- |
| B站 | TikHub 用户视频列表 + 视频详情 | 详情用于补全点赞、投币、分享等；存在共享视频关联 |
| 抖音 | TikHub 账号资料与公开视频列表 | 当前公开响应通常不提供播放、下载 |
| 视频号 | TikHub 账号资料与公开视频列表 | 播放、下载是否返回取决于上游与账号权限 |
| CSDN | 公开主页和文章列表 | 逐篇阅读、点赞、评论、收藏可用，分享通常不公开 |
| 电子发烧友 | 公开作者主页分页 | 页面上限或中断会标记“部分”并合并历史缓存 |
| 百家号 | 公开作者页和文章标签页 | 逐篇互动通常不公开；累计内容可能包含其他体裁 |
| 知乎 | TikHub 文章列表 | 阅读量不公开，资料接口可能受限 |
| 服务号 / 订阅号 | TikHub 或人工后台导入 | 默认不逐篇补互动；历史分页可能受额度限制 |
| 今日头条 | 公开作者页浏览器直采 | 不使用 TikHub；页面结构或访问校验可能导致部分覆盖 |
| 搜狐 | 公开作者主页 | 通常只公开最近一段内容 |
| 小红书 | TikHub 公开笔记列表 | 阅读量不公开 |

当前使用的接口已按官方文档核对：

- [B站用户视频列表](https://docs.tikhub.io/382707665e0)
- [知乎用户文章列表](https://docs.tikhub.io/438163230e0)
- [小红书用户笔记列表](https://docs.tikhub.io/420136396e0)

## 4. 页面图表映射

| 组件 | 回答的问题 | 数据口径 |
| --- | --- | --- |
| 数据质量条 | 这批数据是否新鲜、完整、可比较 | 全量快照，不受筛选影响 |
| KPI 指标卡 | 当前筛选范围的规模和互动总量 | 全局去重；总量仅汇总公开值 |
| 平台分布 | 内容主要发布在哪些平台 | 当前筛选的去重发布数，按数量排序 |
| 发布与播放 / 阅读趋势 | 最近 24 个月的发布节奏与公开流量走势 | 月度合计；当月无公开指标时使用空值，不画成 0 |
| 热榜 | 哪些内容在所选指标上表现最高 | 当前筛选、有公开值的前 10 条 |
| 业务线雷达 | 各业务线在单条平均表现上的相对差异 | 有值均值，最高业务线归一为 100 |
| 账号指标对比 | 各账号在同一指标上的差异 | 保留账号关联；缺失指标显示 `-` |
| 账号概览 | 账号归属、平台、状态、覆盖和主要总量 | 当前筛选 / 历史快照并列 |
| 明细表 | 核验单条记录及跳转原文 | 当前筛选的去重记录，可按全部列排序 |

## 5. 质量保护

- 写出 JSON 和 `file://` 兼容数据脚本时使用同目录临时文件、`fsync` 和原子替换，避免页面读到半截文件。
- HTTP 页面始终以 `no-store` 和时间戳参数读取 JSON；只有直接双击 HTML 时才动态加载 JavaScript 数据文件。
- 每个账号记录 `last_attempt_at`、`last_success_at`、`refreshed_in_run`、请求数和耗时。
- 单账号采集失败时保留最近成功快照并标记 `stale`，不会清空大屏。
- 对历史记录数不少于 20 的账号，若新结果下降到上次的一半以下，会拒绝覆盖并回退缓存。
- “部分”采集若少于历史快照，会按账号和内容 ID 合并未翻到的历史记录，并标记 `snapshot_state: cached`。
- 写出前校验账号主键、外键、重复、发布时间、负数或非法指标，并生成 `quality` 摘要。

## 6. 最新审计基线（2026-08-06）

- 视频：7 个账号，294 条账号关联，292 条唯一视频，2 条共享视频。
- 视频指标覆盖：播放 123/294；点赞、评论、分享、收藏 294/294；下载 0/294。
- 图文：17 个账号，6378 篇唯一文章；13 个账号完整、4 个账号部分覆盖。
- 图文指标覆盖：阅读 2659/6378、点赞 2744/6378、评论 2764/6378、收藏 2324/6378、分享 328/6378。
- 结构校验未发现孤立记录、空主键、账号内重复、负数指标或未来日期。

以上数量会随每次真实刷新变化；以快照中的 `quality` 字段和页面数据质量条为准。

## 7. 验证命令

```bash
python3 -m py_compile pipeline/fetch_data.py pipeline/fetch_article_data.py \
  pipeline/snapshot_utils.py pipeline/validate_snapshots.py
python3 -m unittest discover -s tests -v
node --check web/js/app.js
node --check web/articles/js/app.js
python3 pipeline/validate_snapshots.py
```
